node_modules/
dist/
.env*
# The compact dataset the app consumes is build output derived from data/compounds.json.
# It is regenerated on every build and must never be hand-edited, so it is not committed.
data/generated/
